So, uh, this is where files needed for an example go.

The `rocketchat.sh` shell file gives an example of actually _using_ this stupid namespace, once it's all set up and happy-ified.

The `sudoers_entry` file gives an example of setting up the NOPASSWD chains needed to make this play smoothly in sudoers.

The `br0` file is an example of an `/etc/network/interfaces.d/` config file to set up the bridge.

